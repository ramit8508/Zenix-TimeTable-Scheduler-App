# Environment Configuration for StudentOS Timetable App (Offline SQLite Version)
# IMPORTANT: Change these values before deployment!

# Server Configuration
PORT=5000
NODE_ENV=production

# JWT Secret Key - MUST CHANGE THIS FOR SECURITY
# Generate a random secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_SECURE_RANDOM_STRING_AT_LEAST_64_CHARACTERS_LONG

# Database auto-save interval (milliseconds)
DB_SAVE_INTERVAL=5000
